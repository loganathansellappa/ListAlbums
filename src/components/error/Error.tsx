import React from 'react';
import './Error.scss';

const Error = () => {
  return (
    <div className={'error'}>
      <h1 className={'errorTitle'}>Something Went wrong, refresh the page </h1>
    </div>
  );
};

export default Error;
